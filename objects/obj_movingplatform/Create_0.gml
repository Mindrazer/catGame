    // Movement parameters
    h_speed = 1;  // Horizontal speed
    v_speed = 0;  // Vertical speed
    
    // Movement limits
    x_start = x;
    x_end = x + 200;  // Move 200 pixels to the right
    direction = 1;    // 1 for right, -1 for left